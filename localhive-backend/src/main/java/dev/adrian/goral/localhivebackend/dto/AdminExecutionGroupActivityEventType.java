package dev.adrian.goral.localhivebackend.dto;

public enum AdminExecutionGroupActivityEventType {
    GROUP_CREATED,
    GROUP_SCHEDULING,
    GROUP_RUNNING,
    GROUP_MERGING,
    GROUP_SUCCEEDED,
    GROUP_PARTIALLY_FAILED,
    GROUP_FAILED,
    GROUP_CANCELLING,
    GROUP_CANCELLED,
    GROUP_EXPIRED,
    SHARD_CREATED,
    SHARD_ASSIGNED,
    SHARD_CLAIMED,
    SHARD_RUNNING,
    SHARD_SUCCEEDED,
    SHARD_FAILED,
    SHARD_CANCELLED,
    SHARD_EXPIRED,
    MERGE_CREATED,
    MERGE_ASSIGNED,
    MERGE_CLAIMED,
    MERGE_RUNNING,
    MERGE_SUCCEEDED,
    MERGE_FAILED,
    MERGE_CANCELLED,
    MERGE_EXPIRED,
    ARTIFACT_UPLOADED
}
